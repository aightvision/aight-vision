# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Claude Code local state
.claude/

# Wrangler local dev
.wrangler/
.dev.vars

# Node (in case wrangler gets installed later)
node_modules/

# Env files (secrets never in repo)
.env
.env.*
!.env.example

# Local video files never belong in the repo — they go to R2 via the upload page
aightvision videos/
*.mp4
*.mov
*.MP4
*.MOV
*.webm
*.mkv
*.avi
*.m4v

.dev.vars*
!.dev.vars.example
